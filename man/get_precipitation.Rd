% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_precipitation.R
\name{get_precipitation}
\alias{get_precipitation}
\title{Get precipitation observations}
\usage{
get_precipitation(
  hourly = FALSE,
  daily = FALSE,
  monthly = FALSE,
  fmisid = NULL,
  place = NULL,
  starttime = NULL,
  endtime = NULL,
  crs = NULL,
  bbox = NULL,
  timestep = NULL,
  label = FALSE,
  station = FALSE
)
}
\arguments{
\item{hourly}{A logical for whether to return hourly precipitation observations.}

\item{daily}{A logical for whether to return daily precipitation observations.}

\item{monthly}{A logical for whether to return monthly precipitation observations.}

\item{fmisid}{numeric FMI observation station identifier
(see \link[fmi2]{fmi_stations}).}

\item{place}{character location name for which to provide data.}

\item{starttime}{character or Date start of the time interval in ISO-format.
character will be coerced into a Date object.}

\item{endtime}{character or Date end of the time interval in ISO-format.
character will be coerced into a Date object.}

\item{crs}{character coordinate projection to use in results.}

\item{bbox}{numeric vector (EXAMPLE) bounding box of area for which to return
data.}

\item{timestep}{numeric the time step of data in minutes.}

\item{label}{A logical for whether to label the variables. Default value is \code{FALSE}.}

\item{station}{A logical for whether to add station info into the data. Default value is \code{FALSE}.}
}
\value{
sf object in a long (melted) form. Observation variables names are
given in \code{variable} column. One of following variables is returned, depending on which
spatial resolution is used:
\describe{
\item{PRA_PT1H_ACC}{Hourly precipitation amount (mm)}
\item{rrday}{Daily precipitation amount (mm)}
\item{rrmon}{Monthly precipitation amount (mm)}
}
}
\description{
Precipitation observations for different spatial resolutions.
}
\details{
\code{get_precipitation()} is a wrapper function for \code{obs_weather_monthly()}, \code{obs_weather_daily()}
and \code{obs_weather_hourly()}. User specifies which spatial resolution (hourly, daily or monthly)
they want the data in. At least one location parameter has to be given. The function can also
label variables by using \code{label_variables()} and add info on station(s) by using \code{add_station()}.
}
\examples{
  \dontrun{
    # Hourly precipitation values from Turku
    y <- get_precipitation(hourly = TRUE, place = "Turku", starttime = "2024-06-23",
                           endtime = "2024-06-30")
    # Daily precipitation values from Helsinki
    y <- get_precipitation(daily = TRUE, place = "Helsinki", starttime = "2024-06-01",
                           endtime = "2024-07-01")
    # Monthly precipitation values from Pori
    y <- get_precipitation(monthly = TRUE, place = "Pori", starttime = "2023-07-01",
                           endtime = "2024-07-01")
  }
}
