% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_temperature.R
\name{get_temperature}
\alias{get_temperature}
\title{Get temperature observations}
\usage{
get_temperature(
  hourly = FALSE,
  daily = FALSE,
  monthly = FALSE,
  fmisid = NULL,
  place = NULL,
  starttime = NULL,
  endtime = NULL,
  crs = NULL,
  bbox = NULL,
  timestep = NULL,
  label = FALSE,
  station = FALSE,
  cache = TRUE,
  cache_dir = NULL
)
}
\arguments{
\item{hourly}{A logical for whether to return hourly precipitation observations.}

\item{daily}{A logical for whether to return daily precipitation observations.}

\item{monthly}{A logical for whether to return monthly precipitation observations.}

\item{fmisid}{numeric FMI observation station identifier
(see \link[fmi2]{fmi_stations}).}

\item{place}{character location name for which to provide data.}

\item{starttime}{character begin of the time interval in ISO-format.}

\item{endtime}{character end of time interval in ISO-format.}

\item{crs}{character coordinate projection to use in results.}

\item{bbox}{numeric vector (EXAMPLE) bounding box of area for which to return
data.}

\item{timestep}{numeric the time step of data in minutes.}

\item{label}{A logical for whether to label the variables. Default value is \code{FALSE}.}

\item{station}{A logical for whether to add station info into the data. Default value is \code{FALSE}.}

\item{cache}{A logical whether to do caching. Default is \code{TRUE}.}

\item{cache_dir}{A path to to cache directory. \code{NULL} (default) creates a "fmi2" directory in
the temporary directory defined by base R \code{\link[=tempdir]{tempdir()}} function and uses this directory to
cache data in.}
}
\value{
sf object in a long (melted) form. Observation variables names are
given in \code{variable} column. The returned variables depend on the spatial resolution used.
\itemize{
\item Hourly:
\describe{
\item{TA_PT1H_AVG}{Air temperature (degC)}
\item{TA_PT1H_MAX}{Highest temperature (degC)}
\item{TA_PT1H_MIN}{Lowest temperature (degC)}
}
\item Daily:
\describe{
\item{TG_PT12H_min}{Ground minimum temperature (degC)}
\item{tday}{Air temperature (degC)}
\item{tmax}{Maximum temperature (degC)}
\item{tmin}{Minimum temperature (degC)}
}
\item Monthly:
\describe{
\item{tmon}{Monthly mean temperature}
}
}
}
\description{
Temperature observations for different spatial resolutions.
}
\details{
\code{get_temperature()} is a wrapper function for \code{obs_weather_monthly()}, \code{obs_weather_daily()}
and \code{obs_weather_hourly()}. User specifies which spatial resolution (hourly, daily or monthly)
they want the data in. At least one location parameter has to be given. The function can also
label variables by using \code{label_variables()} and add info on station(s) by using \code{add_station()}.
}
\examples{
  \dontrun{
    # Hourly temperature values from Turku
    y <- get_temperature(hourly = TRUE, place = "Turku", starttime = "2024-06-01",
                         endtime = "2024-06-07")
    # Daily temperature values from Helsinki
    y <- get_temperature(daily = TRUE, place = "Helsinki", starttime = "2024-06-01",
                         endtime = "2024-07-01")
    # Monthly temperature values from Tampere
    y <- get_temperature(monthly = TRUE, place = "Tampere", starttime = "2023-06-01",
                         endtime = "2024-06-01")
  }
}
