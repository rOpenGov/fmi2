% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_airquality.R
\name{get_airquality}
\alias{get_airquality}
\title{Hourly air quality observations}
\usage{
get_airquality(
  starttime = NULL,
  endtime = NULL,
  fmisid = NULL,
  place = NULL,
  parameters = NULL,
  crs = NULL,
  bbox = NULL,
  timestep = NULL,
  geoid = NULL,
  wmo = NULL,
  cache = TRUE,
  cache_dir = NULL
)
}
\arguments{
\item{starttime}{character or Date start of the time interval in ISO-format.
character will be coerced into a Date object.}

\item{endtime}{character or Date end of the time interval in ISO-format.
character will be coerced into a Date object.}

\item{fmisid}{numeric FMI observation station identifier
(see \link[fmi2]{fmi_stations}).}

\item{place}{character location name for which to provide data.}

\item{parameters}{character vector of parameters to return (see below).}

\item{crs}{character coordinate projection to use in results.}

\item{bbox}{numeric vector (for example \code{c(22, 64, 24, 68)}) bounding box of area for
which to return data.}

\item{timestep}{numeric the time step of data in minutes.}

\item{geoid}{numeric geoid of the location for which to return data.}

\item{wmo}{numeric WMO code of the location for which to return data.}

\item{cache}{A logical whether to do caching. Default is \code{TRUE}.}

\item{cache_dir}{A path to to cache directory. \code{NULL} (default) creates a "fmi2" directory in
the temporary directory defined by base R \code{\link[=tempdir]{tempdir()}} function and uses this directory to
cache data in.}
}
\value{
sf object in a long (melted) form. Observation variables names are
given in \code{variable} column. Following variables are returned:
\describe{
\item{AQINDEX_PT1H_avg}{Air quality index}
\item{CO_PT1H_avg}{Carbon monoxide (ug/m3)}
\item{NO2_PT1H_avg}{Nitrogen dioxide (ug/m3)}
\item{NO_PT1H_avg}{Nitrogen monoxide (ug/m3)}
\item{O3_PT1H_avg}{Ozone (ug/m3)}
\item{PM10_PT1H_avg}{Particulate matter < 10 µm (ug/m3)}
\item{PM25_PT1H_avg}{Particulate matter < 2.5 µm (ug/m3)}
\item{QBCPM25_PT1H_AVG}{musta hiili (black coal) PM2.5 (ug/m3)}
\item{SO2_PT1H_avg}{Sulphur dioxide (ug/m3)}
\item{TRSC_PT1H_avg}{Odorous sulphur compounds (ugS/m3)}
}
}
\description{
Hourly air quality observations from Finnish municipalities.
}
\details{
Default set contains 10 different air quality variables.
By default, the data is returned from the last 24 hours. The maximum time interval
for observations is 168 hours (7 days). At least one location
parameter (geoid/place/fmisid/wmo/bbox) has to be given.

The FMI WFS stored query used by this function is
\verb{urban::observations::airquality::hourly::simple}. For more informations, see
\href{https://en.ilmatieteenlaitos.fi/open-data-manual-fmi-wfs-services}{the FMI documentation page}.
}
\examples{
  \dontrun{
    # Get air quality observations from Turku
    y <- get_airquality(place = "Turku", starttime = "2024-06-23", endtime = "2024-06-30")
  }

}
