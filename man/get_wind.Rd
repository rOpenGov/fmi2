% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_wind.R
\name{get_wind}
\alias{get_wind}
\title{Get hourly wind observations}
\usage{
get_wind(
  fmisid = NULL,
  place = NULL,
  starttime = NULL,
  endtime = NULL,
  crs = NULL,
  bbox = NULL,
  timestep = NULL,
  label = FALSE,
  station = FALSE
)
}
\arguments{
\item{fmisid}{numeric FMI observation station identifier
(see \link[fmi2]{fmi_stations}).}

\item{place}{character location name for which to provide data.}

\item{starttime}{character begin of the time interval in ISO-format.}

\item{endtime}{character end of time interval in ISO-format.}

\item{crs}{character coordinate projection to use in results.}

\item{bbox}{numeric vector (EXAMPLE) bounding box of area for which to return
data.}

\item{timestep}{numeric the time step of data in minutes.}

\item{label}{A logical for whether to label the variables. Default value is \code{FALSE}.}

\item{station}{A logical for whether to add station info into the data. Default value is \code{FALSE}.}
}
\value{
sf object in a long (melted) form. Observation variables names are
given in \code{variable} column. Following variables are returned:
\describe{
\item{WD_PT1H_AVG}{Wind direction (deg)}
\item{WS_PT1H_AVG}{Wind speed (m/s)}
\item{WS_PT1H_MAX}{Maximum wind speed (m/s)}
\item{WS_PT1H_MIN}{Minimum wind speed (m/s)}
}
}
\description{
Retrieve hourly wind observations from weather stations.
}
\details{
\code{get_wind()} is a wrapper function for \code{obs_weather_hourly()}. Function returns all wind
related parameters given by \code{obs_weather_hourly()}. At least one location parameter has to be given.
By default data is returned from the last 24 hours. The function can also
label variables by using \code{label_variables()} and add info on station(s) by using \code{add_station()}.
}
\examples{
  \dontrun{
    # Get wind data from Turku
    y <- get_wind(place = "Turku", starttime = "2024-06-23", endtime = "2024-06-30")
  }
}
